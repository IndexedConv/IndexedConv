# .readthedocs.yml

version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.6"  # Update this to the Python version you need, 3.6 is also fine if that's what you require

python:
  install:
    - requirements: docs/doc_requirements.txt
    - path: .
